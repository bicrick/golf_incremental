---
description: Always compile and verify Godot changes via CLI before claiming work is done
alwaysApply: true
---

# Godot CLI Verification

After any GDScript, scene (`.tscn`), or project changes, **run verification before claiming done**. Fix parse/compile errors before finishing. Do not claim success without at least headless smoke + relevant verify scripts.

## Godot binary

```bash
GODOT="/Applications/Godot.app/Contents/MacOS/Godot"
```

Run from project root (`/Users/pbrown/Desktop/golf_incremental`).

## Required commands

**Headless smoke** (always run):

```bash
$GODOT --headless --path . --quit-after 2
```

**Import assets** (when audio, textures, or other imported assets changed):

```bash
$GODOT --headless --import --path . --quit-after 1
```

**Verify** (always, after gameplay/UI changes):

```bash
$GODOT --headless --path . --script res://tools/verify_tour.gd
```

**Pacing** (after economy/upgrade/range changes; target 20–30 min for a good player):

```bash
SKILL=good $GODOT --headless --path . --script res://tools/sim_tour_pacing.gd
```

**Screenshots / full run** (after visual changes): `tools/tour_shot.gd`, `tools/tour_autoplay.gd`.
